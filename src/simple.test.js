import { simpleFn } from './simple';

test("SimpleTest Suite1", () => {
  const simpleAnswer = simpleFn(1);
  expect(simpleAnswer).toBe(1);
});

test("SimpleTest Suite2", () => {
  const simpleAnswer = simpleFn(1);
  expect(simpleAnswer).toBe(1);
});


test("SimpleTest Suite3", () => {
  const simpleAnswer = simpleFn(1);
  expect(simpleAnswer).toBe(1);
});


test("SimpleTest Suite4", () => {
  const simpleAnswer = simpleFn(1);
  expect(simpleAnswer).toBe(1);
});
